import React from "react";

export default function Footer({ height }) {
  return (
    <div
      style={{
        backgroundColor: "#354E57",
        height: height,
        width: "100%",
        marginBottom: "auto",
      }}
    ></div>
  );
}
